# Make sure PyTorch CPU wheels resolve cleanly
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
streamlit==1.35.0
numpy>=1.26
pandas>=2.2
Pillow>=10.3

# OpenCV (headless = no libGL needed)
opencv-python-headless==4.9.0.80

# YOLO / Torch (CPU)
ultralytics==8.2.0
torch==2.2.2
torchvision==0.17.2

# Utils (optional)
matplotlib>=3.8
supervision==0.17.0
ffmpeg-python>=0.2.0
onnxruntime>=1.16.0
